# Usage

- Based on user's arguments, this will creat a set of {number_of_subnets} for each layer
- The number of layer determines the form of this VPC: public, public and private, public, private and isolated (for database)
- Currently: can control the creation and usage of elastic IP and AWS managed NAT